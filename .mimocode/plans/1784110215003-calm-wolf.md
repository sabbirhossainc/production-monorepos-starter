# Fix: pnpm lint failure + Commit changes

## Changes Made

### 1. tsconfig.json fixes
- Added `include` and `exclude` fields to both app tsconfigs to fix path resolution

### 2. ESLint config fixes
- Changed `"lint": "next lint"` to `"lint": "eslint ."` in both app `package.json` files
- Converted shared ESLint config from old `extends` format to ESLint 9+ flat config
- Upgraded `eslint-config-next` from `^15.0.0` to `^16.0.0` for flat config support
- Scoped custom TypeScript rules to `.ts`/`.tsx` files

## Commit

Stage all changed files and create a commit with descriptive message.

## Files changed
- `apps/web/tsconfig.json`
- `apps/snippet-manager/tsconfig.json`
- `apps/web/package.json`
- `apps/snippet-manager/package.json`
- `packages/eslint-config/index.js`
- `packages/eslint-config/package.json`
